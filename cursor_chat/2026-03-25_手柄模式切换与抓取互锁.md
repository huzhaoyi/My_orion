# 2026-03-25 手柄模式切换与抓取互锁

## 进展

在 **orion_joy_arm_bridge** 的 `joy_manipulator_node` 中：**模式切换**时打印明确日志（AUTO↔MANUAL，并说明手动下禁止用 pick_trigger 抓取、自动下允许）；**手动模式**下右手抓取键仅打 **warn** 且不发布 `/manipulator/pick_trigger`；**自动模式**下上升沿照常发布并 **info**。

## 说明

互锁目前作用在 **手柄节点自身**（不发往 MTC）；若需网页或其它节点在手动时也无法 `pick_trigger`，需在 **orion_mtc** 增加订阅/参数门控。
