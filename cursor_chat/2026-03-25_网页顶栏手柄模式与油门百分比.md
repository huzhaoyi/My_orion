# 2026-03-25 网页顶栏手柄自动/手动与油门百分比

## ROS

- `joy_manipulator_node`：`publish_ui_status`（默认 true）下发布  
  - `topic_ui_manual_mode`（默认 `/joy_manipulator/manual_mode`，`std_msgs/Bool`，手动=true）  
  - `topic_ui_throttle_percent`（默认 `/joy_manipulator/throttle_percent`，`std_msgs/Float32`，臂油门 0～100）  
- 控制周期内随左手柄与 `speed_axis_index` 更新。

## Web

- `wsClient` 订阅上述话题；`?joy_ui=` 覆盖前缀（默认 `/joy_manipulator`）。
- `stateStore`：`joyManualMode`、`joyThrottlePercent`；断连置空。
- `TopBar`：徽标「手动/自动/手柄 —」与「油门 N%」。

## 配置

- `joy_manipulator.yaml` 增加 `publish_ui_status` 与两话题名。
