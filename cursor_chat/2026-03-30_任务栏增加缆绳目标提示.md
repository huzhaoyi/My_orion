# 任务栏增加「目标（缆绳）」工作空间提示

## 需求

在工作空间区域除原「工作空间 X/Y/Z」外，增加 **目标点（缆绳）** 与后端 `object_pose` / `feasibility` 一致的提示。

## 实现

- **`RightPanel.js`**：`workspace-hint-stack` 内两行——① 原 TCP 示意 AABB；② **目标（缆绳）**：‖p‖、`Z`、软 ‖p‖，数据来自 `feasibilityWorkspace.js`。
- **`theme.css`**：`.workspace-hint-stack` 纵向间距。
- **`docs/workspace_limits.md`**：任务栏说明更新。
