# 2026-03-25 手动速度度每秒参数与说明（已合并）

早期方案为 `manual_arm_max_speed_rad_s` + 可选 `*_deg_s` 覆盖。  
**当前已全部改为仅 `manual_arm_max_speed_deg_s` / `manual_gripper_max_speed_deg_s`（度/秒）。**

详见：`2026-03-25_手柄参数统一为度.md`。
