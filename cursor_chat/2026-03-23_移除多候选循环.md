# 移除侧抓多候选循环

## 变更

`TaskManager::handlePick` 中删除 `for (i … candidates.size())` 及 `fail_reasons` 汇总统计；`generateCableSideGrasps` 仍返回向量，但**只使用 `candidates[0]`** 做预检、规划、执行。

- 无候选：`CABLE_SIDE_GRASP: 无侧抓候选`
- 预检/初始化/规划/执行失败：各自 `setStateError` 后直接 `return false`
- 未检测到夹持：仍 `retreatToReady()` 后 IDLE + `last_error_`，与此前一致

## 编译

`colcon build --packages-select orion_mtc` 已通过。
