# 移除放置能力、冗余清理与 README（2026-03-23）

## 当日进展

完成 **orion_mtc** 去除全部**放置**相关语义：`RobotTaskMode::PLACING`、`canAcceptPlace`、`/manipulator/place_pose` 订阅、`PerceptionSnapshot::place_pose`、`DecisionFacade`（未使用门面）、`core/place_types.hpp`；精简 **ManipulatorStateMachine** 仅保留抓取相关；**tryGoToReady** 仅拒绝 PICKING；**RuntimeStatus.msg** 注释与 **Web**（TopBar、RuntimeStatusCard）去掉 PLACING；删除未使用成员 **`do_task_running_`**；注释修正 **held_object**、**planning_scene_manager**。**README** 已重写：包说明、分层架构摘要、`object_pose` / `object_axis` / `target_set` / `pick_trigger` 表、测试段增加 `ros2 interface show TargetSet`。

## 完成情况

**已完成**；`colcon build --packages-select orion_mtc` / `orion_mtc_msgs` 通过。

## 问题/需求

无。历史 `cursor_chat` 中若仍出现 place_pose 描述，以当前代码与 README 为准。
