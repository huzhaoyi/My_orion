# 2026-03-23 工作总结（19 / 20 / 23 日）

## 3 月 19 日

- 仓库 `cursor_chat` 中无 `2026-03-19` 对应文档，当日内容无法从本记录还原。

## 3 月 20 日

- **cable_seg 场景**：修复 `executePickSolution` 与 `executeSubTrajectory` 对缆绳段重复 REMOVE 导致的 MoveIt 告警；去掉重复 `removeWorldObject` 及相关参数传递。
- **handlePick 死锁**：成功分支在持 `state_mutex_` 时调用 `setState`/`getHeldObject` 导致自死锁；改为锁内仅更新并拷贝 `held_object_`，锁外 `setState` 与回调。
- **executePickSolution**：说明 DONE 与段后处理的时间窗；补充段后处理诊断日志；确认 `gripper_tcp`、缆绳 id 移除、阶段名等与现状一致。
- **构建**：`colcon build --packages-select orion_mtc` 通过。

## 3 月 23 日

- **架构与消息**：orion_mtc 分层重构、`TargetSet`、`reach_kinematics`、ManipulatorRosInterface 等（详见 `2026-03-23_七层架构重构orion_mtc.md`）。
- **移除放置**：配置 `pick_params.yaml`、launch `pick_holoocean`、前后端与桥接清理放置相关接口。
- **侧抓与 IK**：IK 种子与模型系对齐、`joint_states` 订阅；max_reach 由 URDF 自动上界与余量；预检与 TCP 距离、roll 候选等调优。
- **运维**：急停 `/manipulator/emergency_stop`、回 ready `/manipulator/go_to_ready` 与前端按钮。
- **测试**：编译与 MoveIt 冒烟通过；侧抓统计需 HoloOcean 联调验证。
