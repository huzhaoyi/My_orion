# 2026-03-30 web/js 全量 JSDoc 补全

## 内容

应前轮「希望」延续需求，已为以下文件关键函数补充 **JSDoc**（含修复 `wsClient.js` 误替换导致的 `callService`/`callEmergencyStop` 重复定义问题）：

- `data/wsClient.js`：重连、订阅、推断类型、handleMessage、send、callService、buildPoseStamped、submitJob、各 get_*、checkPick、disconnect
- `layout/TopBar.js`、`LeftPanel.js`、`RightPanel.js`、`Viewport3D.js`、`BottomLogPanel.js`
- `panels/*Card.js`：`render` / 辅助函数 / `handlePickClick`
- `data/stateStore.js`：get/set/subscribe、审批与手柄 UI 状态 setter
- `robot/RobotScene.js`：`createScene`
- `robot/RobotModelLoader.js`：`getWorkspaceUrdf`（说明）、`getWorkspaceBoundsScene`、`getWorkspaceBoundsForDoc`、`loadRobotModel`
- `data/labels.js`：`jobTypeLabel`、`stageNameLabel`、`stageStateLabel`

## 说明

未对 **仅内部闭包函数**（如 `BottomLogPanel` 内 `renderEvents`）逐行加注释，避免噪音；若需要可再指定「内部函数也全部 JSDoc」。
