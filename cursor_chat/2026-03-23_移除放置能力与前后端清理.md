# 2026-03-23 移除放置能力与前后端清理

## 当日进展

推进 **My_orion** 的 **MTC / 上位机 / 仿真桥接** 工作，完成去掉全部「放置」能力后的收尾：补全 `orion_mtc_node` 中放置相关删除、前端 Web 与 `wsClient`/`stateStore` 同步 `SubmitJob` 类型（0～4）、`cable_sensor_to_object_pose` 不再发布 `place_pose`、配置由 `pick_place_params.yaml` 迁移为 **`pick_params.yaml`** 并更新 launch、清理 `runtime_policy.yaml` 中已无代码对应的 `allow_place_fallback_to_release`、更新 **README** 与全仓库放置引用扫描。

## 完成情况

**已完成**：`colcon build --packages-up-to orion_mtc orion_holoocean_bridge` 通过。

## 问题/需求

无；若外部脚本或旧 bag/文档仍引用 `/manipulator/place_pose`、`check_place` 或旧 `JOB_TYPE` 枚举，需按需自行更新。
