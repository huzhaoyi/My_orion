# 2026-03-31 双抓取链 MTC 与前端

## 当日进展

推进 **orion_mtc** 的双感知抓取链与 **keypoint_to_arm_tf**、**Web 上位机** 集成：`ManipulationJob` 增加 `GraspSource`（LEGACY / FUSED）；`/manipulator/object_pose_fused`、`/object_axis_fused` 独立缓存；`pick_trigger_fused` 与 `submit_job.grasp_source`、`Pick.action grasp_source` 选用链路；`TaskManager::handlePick` / `executeJob` 按链取 pose+axis；去重区分链路；`keypoint_to_arm_tf` 将中心线结果 TF 到 `base_link` 后发布 fused 话题；前端订阅 `object_pose_fused`、3D 品红球、感知卡片与「视觉/缆绳」「融合中心线」双按钮。

## 完成情况

当日任务 **已完成**（`orion_mtc_msgs` + `orion_mtc` colcon 构建通过）。

## 问题/需求

- 手柄 `joy_manipulator_node` 仍只发 `/pick_trigger`（LEGACY）；若需手柄触发融合链，可后续增加参数映射到 `pick_trigger_fused`。
- 现场需确认 TF：`keypoint` 的 `output_grasp_frame`（默认 `base_link`）与臂系一致。

## 补充：pick_holoocean 一键带 keypoint

- `pick_holoocean.launch.py` 默认 `start_keypoint_to_arm_tf:=true`，包含 `keypoint_arm_tf.launch.py`；`keypoint_use_mock_keypoints` 默认 **`false`**（订阅真实话题）；离线调试用 `keypoint_use_mock_keypoints:=true`。
- `keypoint_arm_tf.launch.py` 新增 `tf_under_manipulator`，与 MoveIt demo 同开时重映射到 `/manipulator/tf`。
- 关闭融合节点：`start_keypoint_to_arm_tf:=false`。
- `keypoint_to_arm_tf` 默认 **`override_all_keypoints_y/z:=false`**，按感知原始坐标转换，不再写死 y/z。
