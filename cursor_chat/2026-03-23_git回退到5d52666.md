# Git 回退到 5d52666

- 已执行：`git reset --hard 5d526665e2c17c6ecd63c5197745b96ed8e4e45f`（提交说明：夹取功能全正常…）
- 当前 `HEAD` 即上述提交；`origin/master` 上若有更新，本地会显示落后，属正常。
- 回退后请用 **`colcon build --packages-up-to orion_mtc`**（或全工作区）重编，以便 `orion_mtc_msgs` 与旧代码中的 Place/CheckPlace 等接口一致；仅编 `orion_mtc` 可能出现链接缺符号。
- `git reset --hard` 不删除未跟踪文件；`cursor_chat/`、`tools/` 等未纳入该提交的本地文件仍保留，可按需自行清理。
