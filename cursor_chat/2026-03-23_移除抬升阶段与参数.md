# 移除缆绳 pick 抬升阶段与参数

## 代码

- **`pick_task_builder.cpp`**：删除 `MoveRelative("lift object", …)` 整段（+Z 笛卡尔抬升）。
- **`task_manager.cpp`**：`PICK_STAGE_NAMES_CABLE_SIDE` 去掉 `"lift object"`。
- **`MTCConfig`**：删除 `lift_object_min_dist`、`lift_object_max_dist`。
- **`mtc_config.cpp`**：删除 declare/load 上述参数。
- **`pick_params.yaml`**：删除 lift 相关键与注释；动态抓取注释去掉「再抬升」。
- **`pick_task_builder.hpp`**：注释改为「短 retreat」。
- **`web/js/data/labels.js`**：删除 `lift_object` 中文映射。
- **`TaskStage.msg`**：注释更新为不列举 `lift_object`。

## 编译

`colcon build --packages-select orion_mtc orion_mtc_msgs` 已通过。
