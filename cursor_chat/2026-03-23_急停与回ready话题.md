# 2026-03-23 急停与回 ready 话题（前后端）

## 当日进展

推进 **orion_mtc** 与 **web 上位机**：新增话题 **`/manipulator/emergency_stop`**（`std_msgs/Empty`）与 **`/manipulator/go_to_ready`**（`std_msgs/Empty`）；急停时取消 FollowJointTrajectory、清空任务队列、并在 solution 分段间可中止；回 ready 复用既有 `retreatToReady()`（Pilz PTP 至 `ready` + 张开手），仅在非 `PICKING` 且 worker 非 `RUNNING_JOB` 时执行。前端 `wsClient` 增加 `publish` / `publishEmergencyStop` / `publishGoToReady`，顶部栏增加「急停」「回 ready」按钮；README 已补充说明。

## 完成情况

**已完成**：`colcon build --packages-select orion_mtc` 通过。

## 问题/需求

外部通过 rosbridge 发布时需保证话题类型为 `std_msgs/msg/Empty`；若 `go_to_ready` 在机械臂忙时被拒绝，日志中会输出 `busy: ...`。
