# 网页 ROV 坐标轴与 base_link 对齐

- `RobotScene.js` 中原点 **base_axes** 已有 `rotation.x = -π/2`（与上一版一致）。
- `Viewport3D.js` 中 **rovAxesGroup** 每帧设置 `quaternion = Z_UP_TO_Y_UP * q(rov_pose_in_base_link)`，与缆绳目标 `composed` 的 ROS→Three 约定一致。
- README 一句补充。
