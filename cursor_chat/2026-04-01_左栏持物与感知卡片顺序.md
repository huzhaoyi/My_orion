# 左栏持物与感知卡片顺序

## 修改

- 文件：`web/js/layout/LeftPanel.js`
- 将左侧栏中 **感知状态**（`PerceptionCard`）与 **持物状态**（`HeldObjectCard`）的挂载顺序对调。
- 现顺序（队列之后）：感知状态 → 持物状态 → 最近错误；此前为持物状态 → 感知状态。

## 说明

仅调整 DOM 挂载顺序，卡片逻辑与订阅未改。
