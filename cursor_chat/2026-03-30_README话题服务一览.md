# README：ROS 话题与服务一览

## 当日进展

推进 **sealien_ctrlpilot_payload_orion** 文档，在 README 中增加 **「ROS 话题与服务（程序默认 · 当前使用）」** 整节：按 orion_mtc、holoocean 桥、手柄、Keypoints 分表列出订阅/发布/Action，并注明 **`pick_holoocean.launch`** 当前仅启 **CableSensor** 链、未启 **TargetSensor** 桥接；补充 **perception_state** MTC 不订阅、**TF** 双栈与 location 并存注意点。

## 完成情况

- 修正 **`go_to_ready`** 在接口表中为 **`std_srvs/Trigger` 服务**（原误写为 `Empty` 话题）。
- 修正 Web 小节：急停/回 ready 为 **服务调用** 非 `Empty` publish。

## 问题/需求

- 若后续 launch 增加 `target_sensor_to_object_pose`，需同步更新 README「当前 pick_holoocean」小节。
