# 网页 3D 基坐标轴与后端 base_link 对齐

- **原因**：`RobotModelLoader` 根节点 `rotation.x = -π/2`（URDF Z-up → Three Y-up），但 `RobotScene` 的 `base_axes` 未加同一旋转，导致「坐标轴」仍是 Three 世界系，与机械臂/ROS `base_link` 不一致。
- **修改**：`baseAxesGroup.rotation.x = -Math.PI / 2`，与 `loadRobotModel` 根一致。
