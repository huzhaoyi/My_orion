# 七层架构重构 orion_mtc（2026-03-23）

## 当日进展

推进 **My_orion / orion_mtc** 的**感知 / 决策 / 规划 / 执行 / 场景 / 编排 / 接口**分层重构：外部 ROS 话题与服务保持兼容并扩展多目标与放置位姿输入；内部将原 `feasibility`、`strategy` 迁入 **decision**；缆绳侧抓 **pregrasp/IK/碰撞预检** 抽至 `decision/cable_side_pick_precheck`；**JobDeduplicator**、**ManipulationStateMachine** 独立；**ManipulatorRosInterface** 承载原节点内全部订阅/服务/Action 与状态发布；**PerceptionSnapshotProvider** + **TargetCache** + **TargetSelector** 支持 `/target_set` 与 `/object_pose` 回退；新增消息 **TargetSet.msg**；`RobotTaskMode` 增加 **PLACING** 与 **canAcceptPlace**；`reach_kinematics` 纳入编译。

## 完成情况

当日任务**基本完成**（`colcon build --packages-select orion_mtc` / `orion_mtc_msgs` 已通过）。

## 问题/需求

- **PLACE / place_trigger** 等业务 Job 类型与 `TaskManager::executeJob` 分支尚未实现，当前仍以缆绳 PICK 为主；后续需接 **PlaceTaskBuilder** 与放置 FSM。
- **GraspRanker / 完整诊断包** 可按文档第四阶段再补。
- README 中接口表可补充 `/manipulator/place_pose`、`/manipulator/target_set` 说明（未自动改 README，避免非需求文档变更）。
