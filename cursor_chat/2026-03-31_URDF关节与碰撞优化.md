# 2026-03-31 URDF 关节与碰撞优化

## 当日进展

推进 **sealien_ctrlpilot_payload_orion / Orion 7R** 的规划模型，按标称机械行程落实 **关节限位**、**腕部 continuous**、**简化碰撞几何**，并同步 MoveIt / 手柄桥 / Web 前端采样逻辑。

## 完成情况

- **已完成**：`src/orion_description/urdf/orion.urdf` — 臂关节 ±58.5°（1.02102 rad）、前臂滚转 ±133.5°（2.32949 rad，相对标称半角各收 1.5°）；`joint_Link5_Link6` 改为 `continuous`；夹爪两指 revolute 限制 ±0.7854 rad（45°，软件包络，需与实机夹爪型号标定）；Link1–Link8 碰撞改为 box/cylinder 近似。
- **已完成**：`rov_urdf/orion_left.urdf`、`orion_right.urdf` — 同上关节与碰撞；`base_link` 增加规划用 box。
- **已完成**：`orion_moveit_config/config/joint_limits.yaml` — 显式 `min_position`/`max_position`，腕关节 `has_position_limits: false`。
- **已完成**：`orion_joy_arm_bridge` — `joy_manipulator.yaml` 与 `joy_manipulator_node.py` 默认 6 轴角度限与 URDF 一致；腕第 6 轴仍 ±180° 手柄包络。
- **已完成**：`web/js/robot/RobotModelLoader.js` — 工作空间粗采样按分关节限位；`web/sync_robot_model.sh` 已执行同步 `web/robot/orion.urdf`。
- **已完成**：`orion_mtc_params.yaml` 中 feasibility 注释更新。

## 问题/需求

- 标称行程按 **对称中点零位** 理解；若厂家零位定义不同，需重算 `lower`/`upper`。
- 夹爪 ±45° 为保守值，**平行/交错爪行程**需按实物或 CAD 再标定；未引入 URDF `mimic`，以免破坏现有 `arm_with_hand` 八轴轨迹约定。
- `feasibility.*` 距离包络未因本次改动重算（连杆长度未变）；若后续收紧碰撞体或标定变化，需重跑 `tools/compute_max_reach_from_urdf.py` 并斟酌 `max_reach_*`。
